1.
import tkinter as tk
from tkinter import ttk
from tkinter.messagebox import showinfo


cool = tk.Tk()
cool.geometry("300x150")

user_label = tk.Label(cool, text = "username")
user_label = pack(pady=10)

pass_label = tk.Label(cool, text = "username")
pass_label = pack(pady=10)

user_entry = tk.Entry(cool)
user_entry.pack(pady=10)

pass_entry = tk.Entry(cool, show='*')
pass_entry.pack(pady=10)
  
def get_input():

  user = entry_user.get()
  pass = entry.pass.get()
  
send_button=tk.Button(cool,text="submit", command=get_entry)
send_button.pack(pady=10)

root.mainloop()
  
#In line 25 i got some errors
